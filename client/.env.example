# Base URL of the backend API.
#
# Local development: leave blank — Vite's dev server proxies /api to
# http://localhost:4000 automatically (see vite.config.js).
#
# Production: set this to your deployed API origin, e.g.
#   VITE_API_BASE_URL=https://api.yourstore.com
VITE_API_BASE_URL=
